spring.application.name=settlement-service
server.port=${SERVER_PORT:8083}

# ADR-001: the service runs HSQLDB in memory and executes the monolith's own
# schema.sql and seed.sql by file path, so the fixture cannot drift from the
# legacy one. legacy.db.dir is relative to services/settlement-service.
legacy.db.dir=../../src/main/resources/db

spring.datasource.url=jdbc:hsqldb:mem:northstar;sql.syntax_ora=false
spring.datasource.driver-class-name=org.hsqldb.jdbc.JDBCDriver
spring.datasource.username=SA
spring.datasource.password=

spring.sql.init.mode=always
spring.sql.init.schema-locations=file:${legacy.db.dir}/schema.sql
spring.sql.init.data-locations=file:${legacy.db.dir}/seed.sql
spring.sql.init.continue-on-error=false
