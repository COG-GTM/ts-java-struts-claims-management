# Open questions for the settlement slice

Questions the code cannot answer. Each one names the rule it blocks, who can
answer it, and what we do until it is answered.

| Id | Question | Blocks | Owner | Interim decision | Status |
| --- | --- | --- | --- | --- | --- |
| OQ-01 | The Struts action falls back to a policy limit of `10000` when the claim or its policy is not found (`SettlementCalculateAction.java`, `double limit = 10000;`). Is that a business rule or a leftover default? | SETTLE-R05 | claims operations SME | Preserve the fallback in the new service, flag it in the known quirks register (QUIRK-03) | open |
| OQ-02 | Every exception in the legacy application ends on `error.jsp` with HTTP 200 and no `ns:error` marker (QUIRK-05), so a non-numeric deductible and a database outage look the same to the caller. Is a distinct validation error acceptable to operations, and should operational failures return a 5xx? | SETTLE-R06 | claims operations SME | Keep the legacy screen, status and empty error list so `settlement_bad_deductible` parity holds; a change needs a change record | open |
| OQ-03 | `calculated_date` on save is the constant `2019-04-01` (QUIRK-07). Should the service write the current date? | SETTLE-R12 | claims operations SME | Keep the constant so `settlement_save` parity holds | open |
| OQ-04 | The service has no login: `AuthFilter` is outside the slice (ADR-001, decision 5 and 8), so `/settlement/calculate`, `/settlement/save` and `/settlement/detail` answer any caller that can reach the port, and `calculated_by` is the `northstar.operator` property rather than the session user. Where does authentication live once the slice is deployed: the legacy `AuthFilter` and session in front of the service, a gateway, or a port of the filter? | deployment of the slice; SETTLE-R12 (`calculated_by`) | security architect, claims operations SME | The service is reached only by the parity harness and tests; no authentication code is added to the slice; raised by pull request review | open |
| OQ-05 | Amounts are parsed with `Double.parseDouble` and have no length limit (QUIRK-08): the legacy screen accepts a parameter of any length, and `1d`, `0x1.0p0`, `Infinity`, `NaN` and padded values are numbers. Should the service cap input length or narrow the accepted syntax, which the legacy screen does not? | SETTLE-R03 v2, SETTLE-R04, SETTLE-R06 | claims operations SME, security architect | Match legacy exactly (ADR-001, decision 8); a cap or a narrower syntax needs a change record and a new rule version; raised by pull request review | open |
